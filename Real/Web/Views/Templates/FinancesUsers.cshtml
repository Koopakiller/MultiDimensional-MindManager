<div class="tile text">
    <h1>Finances: Users</h1>

    <div>
        <table>
            <tr>
                <th>Id</th>
                <th>Name</th>
            </tr>
            <tr *ngFor="let row of users">
                <td>{{row.id}}</td>
                <td>{{row.header}}</td>
            </tr>
        </table>
    </div>
</div>